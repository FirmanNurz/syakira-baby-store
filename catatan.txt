npm init -y
npm i express sequelize bcryptjs pg >>> untuk memunculkan file package dan node_modules
npm i -D sequelize-cli nodemon
touch .gitignore >>> node_modules
npx sequelize init >>> untuk memunculkan folder config, migrations, models, seeders
ubah dahulu config dengan menyesuaikan ya!

mengubah branch
cd /c/Users/Vrman/phase2/week3/day2/iProject
ls -la
git init >>> karena tidak ada file .git didalamnya setelah dicek
git checkout -b syakira-baby-store
git branch >>> untuk memastikan bahwa branch telah berubah

npx sequelize model:create --name User --attributes name:string,email:string,password:string
npx sequelize model:create --name Category --attributes name:string,UserId:integer
npx sequelize model:create --name Product --attributes imageUrl:string,name:string,price:integer,detail:string,CategoryId:integer,UserId:integer
npx sequelize db:create >>> membuat database syakiraBabyStore
